<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.MainActivity">

    <TextView
        android:id="@+id/tv_test_result"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/tv_no_results_yet"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/et_address"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:inputType="textUri"
        android:text="@string/et_addr_default"
        android:hint="@string/et_addr_hint"
        app:layout_constraintLeft_toLeftOf="@id/tv_test_result"
        app:layout_constraintRight_toRightOf="@id/tv_test_result"
        app:layout_constraintTop_toBottomOf="@id/tv_test_result"
        tools:ignore="Autofill" />

    <Button
        android:id="@+id/btn_start"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_start"
        android:layout_marginTop="5dp"
        app:layout_constraintLeft_toLeftOf="@id/et_address"
        app:layout_constraintRight_toRightOf="@id/et_address"
        app:layout_constraintTop_toBottomOf="@id/et_address" />

</androidx.constraintlayout.widget.ConstraintLayout>
